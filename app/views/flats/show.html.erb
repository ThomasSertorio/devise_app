<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @flat.name %>
</p>

<p>
  <strong>Address:</strong>
  <%= @flat.address %>
</p>
<% if user_signed_in? %>
  ^<%= link_to 'Edit', edit_flat_path(@flat) %> |
<% end %>
<%= link_to 'Back', flats_path %>
